prompt = "config.py silently accepts broken config files and the app then crashes much later somewhere else. Make loading fail fast with clear errors instead."
class = "ambiguous"
max_steps = 15
timeout_sec = 300
