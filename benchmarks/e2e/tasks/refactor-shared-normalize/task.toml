prompt = """users.py and orgs.py each carry an identical private _normalize_name helper. Refactor:
- move the logic into a shared textnorm.py module with a public normalize_name
- both users.py and orgs.py must use it; delete the private copies
- behavior stays identical"""
class = "refactor"
max_steps = 20
timeout_sec = 300
