prompt = "Users report that stats.py keeps crashing on real-world input files. Make it robust enough for production use. Sample inputs are in samples/."
class = "ambiguous"
max_steps = 15
timeout_sec = 300
