set -e
# Three branches needing three different kinds of work: a logic fix, a numeric
# rounding fix, and a manifest regenerated by reading both packages. No single
# bulk transformation solves them, which is the one shape where delegation has
# a theoretical case. The prompt never mentions sub-agents.
python3 check.py
