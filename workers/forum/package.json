{
  "name": "reasonix-forum",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:apply:local": "wrangler d1 execute reasonix-forum --local --file=schema.sql",
    "db:apply:remote": "wrangler d1 execute reasonix-forum --remote --file=schema.sql",
    "db:migrate:performance": "wrangler d1 execute reasonix-forum --remote --file=migrate-performance-indexes.sql"
  },
  "dependencies": {
    "hono": "^4.12.34",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.114.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "pnpm": {
    "overrides": {
      "undici": "7.29.0"
    }
  }
}
